const ProviderEngine = require('web3-provider-engine')
const HookedWallet = require('web3-provider-engine/subproviders/hooked-wallet.js')
const util = require('util');

inherits(IFrameSubProvider, Subprovider)

function IFrameSubProvider(trustedHost, readProvider) {
    
}

module.exports = IFrameSubProvider;
