{
  "name": "html-provider",
  "version": "0.1.0",
  "scripts": {
    "serve": "lite-server",
    "serveTest": "lite-server -c test-bs-config.json",
    "build": "browserify -o provider/bundle.js -e provider/src/HtmlProvider.js -s HtmlProvider"
  },
  "devDependencies": {
    "browserify": "^16.2.3",
    "lite-server": "^2.4.0",
    "web3-provider-engine": "^14.1.0"
  }
}
